<UserControl x:Class="Summa.Wpf.Spinner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="14" 
             d:DesignWidth="50">

    <UserControl.Resources>

        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Summa.Wpf;component/Common.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="UpRepeatButtonStyle"
               TargetType="RepeatButton">

                <Setter Property="Content" Value="{StaticResource UpArrowPath}" />

            </Style>

            <Style x:Key="DownRepeatButtonStyle"
               TargetType="RepeatButton">

                <Setter Property="Content" Value="{StaticResource DownArrowPath}" />

            </Style>
            
        </ResourceDictionary>

    </UserControl.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <RepeatButton Name="UpButton"
                      Grid.Row="0"/>

        <RepeatButton Name="DownButton"
                      Grid.Row="1"/>

    </Grid>

</UserControl>
